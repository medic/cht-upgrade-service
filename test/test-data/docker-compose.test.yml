version: '3.9'
services:
  cht-upgrade-service:
    build: ../../
    ports:
      - "5008:5008"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./docker-compose:/docker-compose
    command: node /app/src/index.js
    environment:
      - FOO
      - BAR
